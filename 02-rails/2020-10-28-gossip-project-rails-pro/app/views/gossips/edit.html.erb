<h2 style="margin-bottom : 5rem">Modifier un potin (potin n°<%= @gossip.id %>) 
</h2>

<%= form_tag url_for(action: 'update'), method: "patch" do %>
  <%= label_tag 'Titre' %>
  <%= text_field_tag 'title', params[:title], class: "form-control", placeholder: @gossip.title, autocomplete: 'off' %>
  <br />
  <br />
  <%= label_tag 'Contenu' %>
  <%= text_area_tag 'content', params[:content], class: "form-control", placeholder: @gossip.content, autocomplete: 'off' %>
  <br />
  <br />
  <%= submit_tag "Modifier le potin", class: "btn btn-primary" %>  
<% end %>
 <div style="padding-top: 2rem;"><%= link_to "Supprimer le potin", gossip_path(@gossip.id), class: "btn btn-danger", method: "delete" %></div>